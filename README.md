# Twill-On

cd into to the `twill_on` folder within the repository and use the command `flutter run` to run the application. Make sure your simmular is already running.

[Flutter Getting started](https://flutter.dev/docs/get-started/install/macos)
